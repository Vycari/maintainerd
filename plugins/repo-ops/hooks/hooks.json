{
  "description": "Two PreToolUse guards on Bash: pr-template-guard denies a gh pr create/edit whose body is missing a heading the repo's own PR template requires; skip-label-race-guard warns when a gh pr create applies the repo's review-skip label without --draft.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/pr-template-guard.sh\"",
            "timeout": 10
          },
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/skip-label-race-guard.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
